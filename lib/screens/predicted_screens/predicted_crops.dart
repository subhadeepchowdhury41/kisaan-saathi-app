import 'package:flutter/material.dart';

class PredictedCropsScreen extends StatelessWidget {
  const PredictedCropsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
